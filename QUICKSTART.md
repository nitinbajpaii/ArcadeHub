# 🚀 QUICK START GUIDE

## Option 1: Automatic Start (Easiest)

Simply double-click `start.bat` file in the root folder.

This will automatically:
- Start the backend server on port 5000
- Start the frontend app on port 3000
- Open both in separate command windows

## Option 2: Manual Start

### Terminal 1 - Backend:
```bash
cd backend
npm start
```

### Terminal 2 - Frontend:
```bash
cd frontend
npm start
```

## 🎮 Access the Application

Once both servers are running:
- Open browser and go to: `http://localhost:3000`
- Backend API runs on: `http://localhost:5000`

## 📝 First Time Setup

1. Click "Start Playing" or "Sign Up"
2. Create your account with:
   - Username
   - Email
   - Password
3. Login and start playing!

## 🎯 Available Games

1. **Rock Paper Scissors** - Tournament mode
2. **Memory Cards** - Match the pairs
3. **Guess The Number** - Find the secret number

## 🏆 Features to Explore

- **Dashboard** - View your stats and recent games
- **Leaderboard** - See top players
- **Help Us Improve** - Share feedback and suggestions

## ⚠️ Troubleshooting

**Port already in use?**
- Backend: Change PORT in `backend/.env`
- Frontend: It will prompt you to use a different port

**Dependencies missing?**
```bash
cd backend && npm install
cd ../frontend && npm install
```

## 🎨 Default Theme

Dark neon gaming theme with:
- Electric Purple/Indigo primary colors
- Neon Green/Cyan accents
- Smooth animations and transitions
- Glassmorphism effects

---

**Ready to play? Let's go! 🎮**
